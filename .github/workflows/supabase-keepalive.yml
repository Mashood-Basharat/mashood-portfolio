name: Supabase Keepalive

on:
  schedule:
    - cron: "0 12 * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit heartbeat endpoint
        run: |
          curl -s -o /dev/null -w "%{http_code}" "${{ vars.VERCEL_URL }}/api/heartbeat"
